import React from 'react';
import CategoryLayout from './CategoryLayout';

const WoodenWork = (props) => {
  return <CategoryLayout category="woodenwork" {...props} />;
};

export default WoodenWork;
