import React from 'react';
import CategoryLayout from './CategoryLayout';

const BedroomCupboard = (props) => {
  return <CategoryLayout category="bedroomcupboard" {...props} />;
};

export default BedroomCupboard;
